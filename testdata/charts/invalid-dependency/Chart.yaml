apiVersion: v2
name: invalid-dependency
description: |
  A test Helm chart with a valid dependeny and an invalid dependency.
type: application
version: v0.1.0
appVersion: "1.16.0"
dependencies:
  - name: cert-manager
    version: v1.16.0
    repository: https://charts.jetstack.io
    condition: cert-manager.enabled
  - name: invalid-dependency
    version: v0.1.0
    repository: https://invalid.frantj.cc
    condition: invalid-dependency.enabled
